# fineract-command-core

Filesystem grouping for the command core OSGi split (ADR-022).

| Path | Gradle project |
|------|----------------|
| `api/` | `:fineract-command-core-api` |
| `impl/` | `:fineract-command-core-impl` |

The compatibility façade remains the top-level `:fineract-command` project.
